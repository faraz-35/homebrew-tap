# faraz-35/tap

Homebrew tap for [DayApp](https://getdayapp.vercel.app) — a live today list with auto-journaling.

```bash
brew tap faraz-35/tap
brew install --cask dayapp
```

Apple Silicon (M1 or later) only. Updates ship through Homebrew: `brew upgrade dayapp`.
